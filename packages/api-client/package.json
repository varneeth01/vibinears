{
  "name": "@vibinears/api-client",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit && mkdir -p dist && echo built > dist/.build",
    "lint": "echo \"No lint configured for api-client\"",
    "test": "echo \"No tests for api-client\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vibinears/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
