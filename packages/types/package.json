{
  "name": "@vibinears/types",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit && mkdir -p dist && echo built > dist/.build",
    "lint": "echo \"No lint configured for types\"",
    "test": "echo \"No tests for types\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
